spring:
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

  sql:
    init:
      mode: always

  h2:
    console:
      enabled: true

logging:
  level:
    root: INFO
    com.Assignment.Trading: DEBUG
  file:
    name: logs/app.log